{
  "name": "salesup-capture-desktop",
  "version": "0.1.0",
  "private": true,
  "author": "salesUp Agency <platform@salesup.be>",
  "description": "salesUp Capture — desktop-app: botloze meeting-opname (Plaud-principe), met automatische meetingdetectie",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dist": "electron-builder --mac --win"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0"
  },
  "build": {
    "appId": "be.salesup.capture.desktop",
    "productName": "salesUp Capture",
    "mac": {
      "category": "public.app-category.business",
      "target": [{ "target": "dmg", "arch": ["arm64", "x64"] }],
      "artifactName": "salesUp-Capture-${version}-${arch}.dmg",
      "icon": "build/icon.icns",
      "hardenedRuntime": true,
      "gatekeeperAssess": false,
      "entitlements": "build/entitlements.mac.plist",
      "entitlementsInherit": "build/entitlements.mac.plist",
      "extendInfo": {
        "NSMicrophoneUsageDescription": "salesUp Capture neemt — na jouw start — meetings op voor kwaliteits- en trainingsdoeleinden.",
        "NSCameraUsageDescription": "salesUp Capture kan beeld opnemen tijdens videocalls.",
        "NSScreenCaptureUsageDescription": "salesUp Capture neemt je videocalls op."
      }
    },
    "win": {
      "icon": "build/icon.png",
      "target": ["nsis"],
      "artifactName": "salesUp-Capture-Setup-${version}.exe"
    }
  },
  "dependencies": {
    "@recallai/desktop-sdk": "^2.0.18"
  }
}
