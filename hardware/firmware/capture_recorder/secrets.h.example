// Kopieer naar secrets.h en vul in. secrets.h staat in .gitignore — nooit committen.
#pragma once

#define WIFI_SSID     "JOUW-WIFI"
#define WIFI_PASS     "JOUW-WACHTWOORD"

// Zelfgekozen lange random string; registreer de sha256-hash in de devices-tabel
// (zie supabase/seed_first_org.sql). In de cloud staat alléén de hash.
#define DEVICE_TOKEN  "JOUW-DEVICE-TOKEN"

#define INGEST_URL    "https://plbuczbxtauhuobkicdr.supabase.co/functions/v1/ingest-recording"
